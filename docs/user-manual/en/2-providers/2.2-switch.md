# 2.2 Switch Provider

## Switch from Main Interface

In the provider list, click the "Enable" button on the target provider card.

OpenCode, OpenClaw, Hermes, Pi, and MiniMax Code are coexist apps that can have several providers added at the same time; see [Coexist Apps](#coexist-apps) below for how they work.

### Switching Flow

1. Click the "Enable" button
2. CC Switch updates the configuration file
3. The card status changes to "Currently Active"
4. The change takes effect in the way each app supports (see "Activation Methods" below)

### Status Indicators

| Status | Display | Description |
|--------|---------|-------------|
| Currently Active | Blue border + label | Current provider in the configuration file |
| Routing Active | Green border | The provider actually forwarding requests while local routing is on |
| Normal | Default style | Inactive provider |

## Quick Switch via System Tray

Quickly switch providers via the system tray without opening the main interface.

### Steps

1. Right-click the CC Switch icon in the system tray
2. Hover over the corresponding app submenu (e.g., "Claude · CurrentProvider")
3. Click the provider name you want to switch to
4. Switching completes with a brief tray notification

### Tray Menu Structure

Starting from v3.13.0, the tray menu is refactored from a flat list into **per-app submenus**, with a dedicated submenu for each app:

| Submenu    | Description                                                    |
| ---------- | -------------------------------------------------------------- |
| Claude     | All Claude providers (including Codex OAuth reverse proxy)     |
| Codex      | All Codex providers                                            |
| Gemini     | All Gemini providers                                           |
| Grok Build | All Grok Build providers                                       |

The tray only includes these four apps that support local routing; for Claude Desktop and coexist apps, use the main interface.

**Benefits of the refactor**:

- **Prevents menu overflow**: With many providers, a flat list would exceed screen height; per-app submenus scale naturally
- **Submenu title shows the currently active provider and usage summary**: You know at a glance which provider Claude / Codex / Gemini / Grok Build is using, plus available cached usage information, without opening the submenu
- **Per-app isolation**: Switching Claude's provider doesn't disturb the other apps' views

> 💡 **Tip**: The combination of background residency + Lightweight Mode + per-app submenus is especially suited for heavy users who frequently switch among multiple apps. See [1.5 Personalization → Lightweight Mode](../1-getting-started/1.5-settings.md).

![image-20260108004348993](../../assets/image-20260108004348993.png)

## Activation Methods

### Claude Code

**Takes effect immediately after switching**, no restart needed.

Claude Code supports hot reload and automatically detects configuration file changes and reloads.

### Claude Desktop

Restart Claude Desktop after switching. Providers in "Model Mapping" mode also need CC Switch to keep running; see [2.6 Claude Desktop](./2.6-claude-desktop.md) for details.

### Codex

Requires restart after switching:
- Close the current terminal window
- Reopen the terminal

### Gemini CLI

**Takes effect immediately after switching**, no restart needed.

Gemini CLI re-reads the `.env` file on each request.

### Grok Build

Restart Grok Build after switching; a notification appears when the switch succeeds.

### With Local Routing On

When local routing is on, requests from Claude Code, Codex, Gemini CLI, and Grok Build are all forwarded through CC Switch, so switching providers applies to subsequent requests immediately. However, if the switch changes the model, Codex and Grok Build may still need a restart.

## Configuration File Changes

When switching providers, CC Switch modifies the following files:

### Claude

```
~/.claude/settings.json
```

Modified content:
```json
{
  "env": {
    "ANTHROPIC_API_KEY": "new API Key",
    "ANTHROPIC_BASE_URL": "new endpoint"
  }
}
```

### Codex

```
~/.codex/config.toml
```

A third-party provider's API Key is written to that provider's `experimental_bearer_token` in `config.toml`, not to `~/.codex/auth.json`. `auth.json` is used only for the official OpenAI ChatGPT login: whether it is kept when you switch to a third-party provider is controlled by "Settings → General → Codex App Enhancements". Providers with a model mapping also generate `~/.codex/cc-switch-model-catalog.json`.

### Gemini

```
~/.gemini/.env
~/.gemini/settings.json
```

### Grok Build

```
~/.grok/config.toml
```

### Claude Desktop

Writes Claude Desktop's 3P profile configuration (on macOS under `~/Library/Application Support/Claude-3p/`); see [2.6 Claude Desktop](./2.6-claude-desktop.md) for details.

## Coexist Apps

OpenCode, OpenClaw, Hermes, Pi, and MiniMax Code let multiple providers be written into the tool's own configuration at the same time, and you choose which one to use inside the tool:

| App | Buttons | Written to |
|-----|---------|------------|
| OpenCode | Add / Remove | `~/.config/opencode/opencode.json` |
| OpenClaw | Add / Remove | `~/.openclaw/openclaw.json` |
| Hermes | Add / Remove | `custom_providers` in `~/.hermes/config.yaml` |
| Pi | Enable / Remove | `~/.pi/agent/models.json` |
| MiniMax Code | Add / Remove | `custom_provider` in `~/.minimax/config.yaml` |

After adding:

- **Hermes**: the card also has an "Enable / In Use" button; clicking "Enable" sets `model.provider` and `model.default` in `config.yaml` to that provider
- **OpenClaw**: the card also has a "Set Default / Current Default" button for setting OpenClaw's default model
- **OpenCode, Pi, MiniMax Code**: choose the model to use inside the tool

Coexist apps do not support tray switching or the failover queue. In most cases you can delete a provider directly, with these exceptions:

- The provider Hermes is currently using, and read-only providers managed by Hermes itself
- A provider referenced by OpenClaw's default model
- A provider referenced by MiniMax Code's `defaultModel` / `defaultLightModel`
- Pi: the delete button is unavailable until Pi's own state has been read

## Handling Switch Failures

If switching fails, possible reasons:

### Configuration File Is Locked

Another program is using the configuration file.

**Solution**: Close the running CLI tool and try switching again.

### Insufficient Permissions

No write permission to the configuration file.

**Solution**: Check the permission settings of the configuration directory.

### Invalid Configuration Format

The provider's JSON configuration has format errors.

**Solution**: Edit the provider, check and fix the JSON format.
