# 1.4 クイックスタート

このセクションでは、5 分で初回設定を完了する方法を説明します。

## ステップ 1：プロバイダーの追加

1. メイン画面右上の **+** ボタンをクリック
2. 「プロバイダータイプ」からプロバイダーを選択
   - よく使われるプリセット：Zhipu GLM、MiniMax、DeepSeek、Kimi、PackyCode
   - または「カスタム」を選択して手動設定
3. **API Key** を入力
4. 「追加」をクリック

![image-20260108002807657](../../assets/image-20260108002807657.png)

> 💡 **ヒント**：プリセットではエンドポイントアドレスが自動入力されるため、API Key のみ入力すれば使用できます。

## ステップ 2：プロバイダーの切り替え

追加が完了すると、プロバイダーがリストに表示されます。

**方法 1：メイン画面で切り替え**
- プロバイダーカードの「有効化」ボタンをクリック
- OpenCode、OpenClaw、Hermes、MiniMax Code は共存型アプリで、ボタンは「追加」（Pi は「有効化」）です。複数のプロバイダーを同時に追加できます

**方法 2：トレイで素早く切り替え**
- システムトレイアイコンを右クリック
- プロバイダー名を直接クリック（Claude Code、Codex、Gemini CLI、Grok Build に対応）

## ステップ 3：反映方法

プロバイダーを切り替えた後、各 CLI ツールの反映方法は異なります：

| アプリ | 反映方法 |
|------|----------|
| Claude Code | ✅ 即時反映（ホットリロード対応） |
| Claude Desktop | Claude Desktop の再起動が必要 |
| Codex | ターミナルの再起動が必要 |
| Gemini | ✅ 即時反映（リクエストごとに設定を再読み込み） |
| Grok Build | Grok Build の再起動が必要 |
| OpenCode | ターミナルの再起動が必要 |
| OpenClaw | ターミナルの再起動が必要 |
| Hermes / Pi / MiniMax Code | プロバイダーを追加するとツールの設定に書き込まれます。Pi と MiniMax Code ではツール側で使用するモデルを選択し、Hermes ではカードの「有効化」をクリックして現在のプロバイダーに設定できます |

ローカルルーティングを有効にしている場合、Claude Code、Codex、Gemini CLI、Grok Build でのプロバイダー切り替えは以降のリクエストに即座に反映されます。ただし、切り替えによってモデルが変わる場合、Codex と Grok Build は再起動が必要になることがあります。

### Claude Code の初回インストール時の注意

Claude Code を初めて起動するときに**ログイン**の要求や初期化ガイドが表示される場合は、CC Switch で「Claude Code の初回確認をスキップ」オプションを有効にしてください：

1. CC Switch の「設定 → 一般」を開く
2. 「Claude Code の初回確認をスキップ」スイッチをオンにする
3. Claude Code を再起動

![image-20260108002626389](../../assets/image-20260108002626389.png)

> ⚠️ **注意**：このオプションは `~/.claude.json` に `hasCompletedOnboarding: true` を書き込み、公式の初回インストール確認フローをスキップします。

## 設定の確認

再起動後、対応する CLI ツールを起動して簡単な質問でテストします：

```bash
# Claude Code - 起動後にテスト質問を入力
claude
> こんにちは、簡単に自己紹介してください

# Codex - 起動後にテスト質問を入力
codex
> こんにちは、簡単に自己紹介してください

# Gemini - 起動後にテスト質問を入力
gemini
> こんにちは、簡単に自己紹介してください

# OpenCode - 起動後にテスト質問を入力
opencode
> こんにちは、簡単に自己紹介してください

# OpenClaw - 起動後にテスト質問を入力
openclaw
> こんにちは、簡単に自己紹介してください
```

AI が正常に回答すれば、設定は成功です。

## 次のステップ

基本設定が完了しました。次に以下のことができます：

- [プロバイダーの追加](../2-providers/2.1-add.md) - 複数のプロバイダーを設定して簡単に切り替え
- [MCP サーバーの設定](../3-extensions/3.1-mcp.md) - AI ツールの機能を拡張
- [システムプロンプトの設定](../3-extensions/3.2-prompts.md) - AI の動作をカスタマイズ
- [ローカルルーティングの有効化](../4-proxy/4.1-service.md) - フォーマット変換が必要なプロバイダーの利用、自動フェイルオーバー
- [使用量統計の確認](../4-proxy/4.4-usage.md) - リクエスト数、Token、費用を集計

## よくある質問

### 切り替えても反映されない場合

ターミナルまたは CLI ツールを再起動してください。設定ファイルは切り替え時に更新されますが、実行中のプログラムは自動的に設定を再読み込みしません。

### プリセットが見つからない場合

プロバイダーがプリセットリストにない場合は、「カスタム」を選択して手動設定してください。設定形式については [プロバイダーの追加](../2-providers/2.1-add.md) を参照してください。

### 公式ログインに戻すには

Claude Code、Claude Desktop、Codex、Gemini CLI、Grok Build のプロバイダーリストには、それぞれ公式プロバイダー（Claude Official、OpenAI Official、Google Official、Grok Official など）が最初から含まれています。そちらに切り替えてクライアントを再起動し、各ツール自身のログインフローに従ってください。リストにない場合（削除した場合や旧バージョンからアップグレードした場合など）は、プリセットから追加し直せます。
