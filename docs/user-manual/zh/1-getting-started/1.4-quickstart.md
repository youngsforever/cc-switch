# 1.4 快速上手

本节帮助你在 5 分钟内完成首次配置。

## 第一步：添加供应商

1. 点击主界面右上角的 **+** 按钮
2. 在「预设供应商」中选择你的供应商
   - 常用预设：智谱 GLM、MiniMax、DeepSeek、Kimi、PackyCode
   - 或选择「自定义」手动配置
3. 填写 **API Key**
4. 点击「添加」

![image-20260108002807657](../../assets/image-20260108002807657.png)

> 💡 **提示**：预设会自动填充端点地址，你只需要填写 API Key。

## 第二步：切换供应商

添加完成后，供应商会出现在列表中。

**方式一：主界面切换**
- 点击供应商卡片的「启用」按钮
- OpenCode、OpenClaw、Hermes、MiniMax Code 是共存式应用，按钮为「添加」（Pi 为「启用」），可以同时添加多个供应商

**方式二：托盘快速切换**
- 右键系统托盘图标
- 直接点击供应商名称（支持 Claude Code、Codex、Gemini CLI、Grok Build）

## 第三步：生效方式

切换供应商后，各 CLI 工具的生效方式不同：

| 应用 | 生效方式 |
|------|----------|
| Claude Code | ✅ 即时生效（支持热重载） |
| Claude Desktop | 需要重启 Claude Desktop |
| Codex | 需要关闭并重新打开终端 |
| Gemini | ✅ 即时生效（每次请求重新读取配置） |
| Grok Build | 需要重启 Grok Build |
| OpenCode | 需要关闭并重新打开终端 |
| OpenClaw | 需要关闭并重新打开终端 |
| Hermes / Pi / MiniMax Code | 供应商添加后写入工具的配置；Pi、MiniMax Code 在工具里选择要使用的模型，Hermes 可在卡片上点击「启用」设为当前供应商 |

开启本地路由后，Claude Code、Codex、Gemini CLI、Grok Build 切换供应商会立即作用于后续请求；但如果切换改变了模型，Codex 和 Grok Build 仍可能需要重启。

### Claude Code 首次安装提示

如果 Claude Code 首次启动时提示需要**登录**或显示初始化引导，请在 CC Switch 中开启「跳过 Claude Code 初次安装确认」选项：

1. 打开 CC Switch「设置 → 通用」
2. 开启「跳过 Claude Code 初次安装确认」开关
3. 重新启动 Claude Code

![image-20260108002626389](../../assets/image-20260108002626389.png)

> ⚠️ **注意**：此选项会在 `~/.claude.json` 中写入 `hasCompletedOnboarding: true`，跳过官方的初次安装确认流程。

## 验证配置

重启后，启动对应的 CLI 工具并输入简单的问题进行测试：

```bash
# Claude Code - 启动后输入测试问题
claude
> 你好，请简单介绍一下自己

# Codex - 启动后输入测试问题
codex
> 你好，请简单介绍一下自己

# Gemini - 启动后输入测试问题
gemini
> 你好，请简单介绍一下自己

# OpenCode - 启动后输入测试问题
opencode
> 你好，请简单介绍一下自己

# OpenClaw - 启动后输入测试问题
openclaw
> 你好，请简单介绍一下自己
```

如果 AI 能正常回复，说明配置成功。

## 下一步

恭喜！你已经完成了基础配置。接下来可以：

- [添加更多供应商](../2-providers/2.1-add.md) - 配置多个供应商方便切换
- [配置 MCP 服务器](../3-extensions/3.1-mcp.md) - 扩展 AI 工具的能力
- [设置系统提示词](../3-extensions/3.2-prompts.md) - 自定义 AI 的行为
- [开启本地路由](../4-proxy/4.1-service.md) - 使用需要格式转换的供应商、自动故障转移
- [查看用量统计](../4-proxy/4.4-usage.md) - 统计请求数、Token 和花费

## 常见问题

### 切换后不生效？

确保重启了终端或 CLI 工具。配置文件在切换时已经更新，但运行中的程序不会自动重新加载。

### 找不到预设？

如果你的供应商不在预设列表中，选择「自定义」手动配置。参考 [添加供应商](../2-providers/2.1-add.md) 了解配置格式。

### 如何恢复官方登录？

Claude Code、Claude Desktop、Codex、Gemini CLI、Grok Build 的供应商列表里都自带一个官方供应商（如 Claude Official、OpenAI Official、Google Official、Grok Official），切换过去后重启客户端，按工具自身的登录流程操作即可。如果列表里没有（例如删掉了，或从旧版本升级），可以从预设里重新添加。
