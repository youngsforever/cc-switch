# 1.5 个性化配置

本节介绍如何根据个人偏好配置 CC Switch。

## 打开设置

- 点击左上角 **⚙️** 按钮
- 或使用快捷键 `Cmd/Ctrl + ,`

## 语言设置

CC Switch 支持四种语言：

| 语言     | 说明     |
| -------- | -------- |
| 简体中文 | 默认语言 |
| 繁體中文 | 繁体中文界面 |
| English  | 英文界面 |
| 日本語   | 日文界面 |

切换语言后立即生效，无需重启。

## 主题设置

| 选项     | 说明                        |
| -------- | --------------------------- |
| 跟随系统 | 自动匹配系统的深色/浅色模式 |
| 浅色     | 始终使用浅色主题            |
| 深色     | 始终使用深色主题            |

## 窗口行为

### 开机自启

开启后，系统启动时自动运行 CC Switch。

- **Windows**：通过注册表实现
- **macOS**：通过 LaunchAgent 实现
- **Linux**：通过 XDG autostart 实现

### 关闭行为

| 「关闭时最小化到托盘」 | 点击窗口关闭按钮时         |
| ---------------------- | -------------------------- |
| 开启（默认）           | 隐藏到系统托盘，继续在后台运行 |
| 关闭                   | 完全退出应用               |

推荐保持开启，方便通过托盘快速切换供应商。

### 轻量模式

v3.13.0 起新增「轻量模式」——一种**仅托盘运行**的状态，用于把空闲时的桌面占用降到最低。

**触发方式**：右键系统托盘图标 → 点击「轻量模式」。主窗口会被**销毁**（而不是隐藏），UI 资源和内存随之释放。

**退出方式**：从托盘菜单点击「打开主界面」，或通过深链接 / 再次启动 CC Switch。窗口会按需**重建**，状态保持一致。

| 特性         | 最小化到托盘         | 轻量模式                 |
| ------------ | -------------------- | ------------------------ |
| UI 进程      | 保留在内存中         | 完全销毁                 |
| 空闲资源占用 | 与正常运行相当       | 接近零                   |
| 再次打开速度 | 瞬时（直接显示）     | 略慢（需要重建窗口）     |
| 托盘切换功能 | 可用                 | 可用                     |
| 深链接唤起   | 可用                 | 可用（按需重建）         |

> 💡 **使用场景**：如果你 CC Switch 长时间常驻后台，主要通过托盘菜单切换供应商，开启轻量模式能显著降低内存占用。

> ⚠️ **注意**：轻量模式状态不持久 — 下次正常启动时会回到普通模式。需要长期使用可搭配开机自启。

### 应用到 Claude Code 插件

开启后，VS Code 中 Claude Code 插件的供应商会随 CC Switch 切换（写入 `~/.claude/config.json` 的 `primaryApiKey`）。

> 💡 **使用场景**：如果你同时使用 Claude Code CLI 和 VS Code 插件，开启此选项可以保持两者配置一致。

### 跳过 Claude Code 初次安装确认

开启后，跳过 Claude Code 首次启动时的初次安装确认，适合已熟悉 Claude Code 的用户。

> ⚠️ **注意**：此选项会在 `~/.claude.json` 中写入 `hasCompletedOnboarding: true`；关闭时删除该字段。

### 主页面显示

选择在主页面的应用切换器中显示哪些应用。每个应用可以独立开关，但至少保留一个。

可配置的应用：Claude Code、Claude Desktop、Codex、Gemini、Grok Build、OpenCode、OpenClaw、Hermes、Pi、MiniMax Code。

同一组里还有 **显示项目切换** 开关，控制主页面顶部是否显示项目切换入口。

> 💡 **使用场景**：如果你只使用 Claude Code 和 Codex CLI，可以隐藏其他应用，保持界面简洁。

### Skills 存储位置

选择 CC Switch 存放 Skills 主副本的目录：

| 选项              | 说明                                                                 |
| ----------------- | -------------------------------------------------------------------- |
| CC Switch（默认） | 存放在 `~/.cc-switch/skills/`，由 CC Switch 统一管理并同步到各应用    |
| `~/.agents/skills` | 存放在 `~/.agents/skills/`，遵循 Agent Skills 开放标准，兼容的工具可以直接读取 |

### Skills 同步方式

设置技能安装到各应用目录时的同步方式：

| 方式              | 说明                                                 |
| ----------------- | ---------------------------------------------------- |
| 软连接            | 创建符号链接指向技能源文件，占用空间小，更新实时同步 |
| 文件复制          | 将技能文件完整复制到目标目录                         |

> 💡 **推荐**：默认使用软连接方式。如果遇到权限问题，可切换为文件复制方式。

### Codex 应用增强

包含两个开关，默认都关闭：

**非接管切换时保留官方登录**：

- **关闭**：未开启本地路由时，每次切换到第三方供应商都会删除整个 `~/.codex/auth.json`（官方登录随之清除）；切回官方后需要重新登录
- **开启**：直接切换到第三方供应商时保留 `~/.codex/auth.json`，切回官方无需重新登录

第三方供应商的 API Key 始终只写入 `~/.codex/config.toml`，不会写进 `auth.json`；开启本地路由后，官方登录始终保留。详见攻略 [使用第三方 API 时保留 Codex 远程操作和官方插件](../../../guides/codex-official-auth-preservation-guide-zh.md)。

**统一 Codex 会话历史**：开启后，官方订阅以共享的 custom 供应商标识运行，官方与第三方会话出现在同一个历史列表中，并可选择把现有官方会话一并迁入（迁移前自动备份）；关闭时可按备份恢复。注意：跨供应商继续旧会话时，对方后端可能无法解密会话中的推理内容，导致继续失败。详见攻略 [统一 Codex 会话历史](../../../guides/codex-unified-session-history-guide-zh.md)。

### 首选终端

选择 CC Switch 打开终端时使用的终端应用程序。

支持的终端（按平台）：

| 平台    | 终端选项                                                           |
| ------- | ------------------------------------------------------------------ |
| macOS   | Terminal、iTerm2、Alacritty、Kitty、Ghostty、Otty、WezTerm、Kaku、Warp |
| Windows | CMD、PowerShell、Windows Terminal                                  |
| Linux   | GNOME Terminal、Konsole、Xfce4 Terminal、Alacritty、Kitty、Ghostty |

## 目录配置

设置 → 高级 → 配置文件目录

### 应用配置目录

CC Switch 自身数据的存储位置，默认为 `~/.cc-switch/`。

### CLI 工具目录

可以自定义各 CLI 工具的配置目录：

| 配置            | 默认值                | 说明                   |
| --------------- | --------------------- | ---------------------- |
| Claude 目录     | `~/.claude/`          | Claude Code 配置目录   |
| Codex 目录      | `~/.codex/`           | Codex 配置目录         |
| Gemini 目录     | `~/.gemini/`          | Gemini CLI 配置目录    |
| Grok Build 目录 | `~/.grok/`            | Grok Build 配置目录    |
| OpenCode 目录   | `~/.config/opencode/` | OpenCode 配置目录      |
| OpenClaw 目录   | `~/.openclaw/`        | OpenClaw 配置目录      |
| Hermes 目录     | `~/.hermes/`          | Hermes 配置目录        |
| Pi 目录         | `~/.pi/agent/`        | Pi 的 agent 配置目录   |

Claude Desktop 和 MiniMax Code 不支持在这里覆盖目录。MiniMax Code 的数据目录默认为 `~/.minimax/`，可以用环境变量 `MINIMAX_DATA_DIR`（或 `MAVIS_DATA_DIR`）指定；Hermes 另外识别 `HERMES_HOME`，Pi 另外识别 `PI_CODING_AGENT_DIR`。

> 💡 **WSL**：CC Switch 不会自动识别 WSL。在 Windows 上可以把目录改成 WSL 里的路径（如 `\\wsl.localhost\Ubuntu\home\<用户名>\.claude`），CC Switch 就会读写 WSL 里的配置，「关于」页也会在对应的 WSL 发行版里检测和升级该工具。

> ⚠️ **注意**：修改目录后需要重启应用，且对应的 CLI 工具也需要配置相同的目录。

## 数据管理

### 导出配置

点击「导出 SQL 备份」按钮，保存包含以下内容的 SQL 备份文件：

- 所有供应商配置
- MCP 服务器配置
- Prompts 预设
- 用量日志
- 应用设置

导出文件名格式为 `cc-switch-export-{timestamp}.sql`。

### 导入配置

1. 点击「选择 SQL 文件」
2. 选择之前导出的备份文件
3. 按钮变为「导入」后，点击它开始导入
4. 导入成功后，界面会显示本次导入前自动创建的备份 ID

> ⚠️ **注意**：导入会覆盖现有配置，建议先导出当前配置作为备份。

## 路由设置

设置 → 路由 Tab

路由 Tab 集中管理本地路由相关功能：

### 本地路由

启动/停止本地路由，按应用开启路由，配置监听地址和端口。详见 [4.1 本地路由服务](../4-proxy/4.1-service.md)。

### 自动故障转移

按应用（Claude / Codex / Gemini / Grok Build）配置故障转移队列和自动切换策略。详见 [4.3 故障转移](../4-proxy/4.3-failover.md)。

### 整流器

自动修复部分上游不兼容的请求，例如 Thinking 签名不兼容时移除相关内容后重试。

### 全局出站代理

配置 CC Switch 的出站 HTTP/HTTPS 代理，适用于需要通过代理访问外部 API 的场景。

## 高级设置

设置 → 高级 Tab

### 配置文件目录

自定义各应用的配置文件目录。详见上方「目录配置」章节。

### 数据管理

导入/导出配置备份。详见上方「数据管理」章节。

### 备份与恢复

备份管理面板提供对数据库备份的全面控制。

#### 自动备份设置

| 配置     | 选项                               | 默认值    |
| -------- | ---------------------------------- | --------- |
| 自动备份间隔 | 禁用、6h、12h、24h、48h、7d        | 24 小时   |
| 备份保留数量 | 3、5、10、15、20、30、50           | 10 个备份 |

设置间隔后，CC Switch 会按计划自动备份数据库。超出保留数量的旧备份会自动删除。

#### 备份列表

面板显示所有现有备份，包含：
- **显示名称**（根据时间戳自动生成，如 `db_backup_20260315_143000`）
- **创建时间**
- **文件大小**（如 "1.5 MB"）

#### 备份操作

| 操作         | 说明                                                                     |
| ------------ | ------------------------------------------------------------------------ |
| **立即备份** | 立即创建一个备份                                                         |
| **恢复**     | 从选定的备份恢复数据库。恢复前会自动创建当前数据库的安全备份             |
| **重命名**   | 修改备份的显示名称                                                       |
| **删除**     | 永久删除备份（需确认）                                                   |

> ⚠️ **重要**：恢复备份会覆盖当前数据库。恢复操作前始终会自动创建安全备份，以便在需要时恢复。

### 云同步

通过 **WebDAV** 或 **S3 兼容存储** 在多台设备间同步配置，两种方式同一时间只能启用一种。使用 **v2 协议**，支持双层版本控制，提高可靠性。

#### WebDAV

| 配置项   | 说明                                                                         |
| -------- | ---------------------------------------------------------------------------- |
| 服务商   | 坚果云 / Nextcloud / 群晖 NAS / 自定义                                         |
| WebDAV 服务器地址 | WebDAV 服务器 URL                                                     |
| WebDAV 账户 | 登录用户名                                                           |
| WebDAV 密码 | 登录密码（应用专用密码；已保存的凭据在未修改时会被保留）                     |
| 远程根目录 | 远程存储路径（默认 `cc-switch-sync`）                                        |
| 同步配置名 | 设备配置文件名（默认 `default`）                                             |
| 自动同步 | 开启后每次数据库变更都会自动上传                                             |

#### S3 兼容存储

| 配置项            | 说明                                                                                  |
| ----------------- | ------------------------------------------------------------------------------------- |
| 服务商            | AWS S3 / MinIO / Cloudflare R2 / 阿里云 OSS / 腾讯云 COS / 华为云 OBS / S3 兼容服务（自定义） |
| 区域 (Region)     | 如 `us-east-1`；R2 填 `auto`                                                           |
| 存储桶 (Bucket)   | 存放同步数据的存储桶                                                                  |
| Access Key ID / Secret Access Key | 访问凭据                                                             |
| Endpoint          | AWS 可留空，多数 S3 兼容服务需要填写                                                  |
| 远程根目录        | 默认 `cc-switch-sync`                                                                 |
| 同步配置名        | 默认 `default`                                                                        |
| 自动同步          | 开启后每次数据库变更都会自动上传                                                      |

#### 操作

| 操作         | 说明                                                                                         |
| ------------ | -------------------------------------------------------------------------------------------- |
| **测试连接** | 验证服务地址和凭据是否有效                                                                   |
| **上传**     | 将本地数据库上传到远程，显示进度指示器                                                       |
| **下载**     | 下载远程数据库。下载前显示远程快照信息（协议版本、数据库版本、时间戳、大小）供确认。覆盖本地数据库前会自动创建安全备份 |

#### 自动同步

启用 **自动同步** 后：
- 首次激活时会显示确认对话框
- 每次数据库变更后，CC Switch 会自动把数据库上传到云端
- 面板中显示上次同步时间；自动同步失败时会显示错误，并在后续变更时继续重试

#### 远程快照信息

下载前，CC Switch 会显示远程快照的详细信息：
- 协议版本（v2）
- 数据库兼容版本
- 远程备份的时间戳
- 文件大小
- 兼容性状态（不兼容时显示警告）

> ⚠️ **注意**：上传会覆盖远程数据，下载会覆盖本地数据。下载前始终会自动创建安全备份。

### 应用诊断日志

| 配置项           | 说明                                                        |
| ---------------- | ----------------------------------------------------------- |
| 启用应用诊断日志 | 控制 `cc-switch.log`；不影响请求用量记录和 `crash.log`       |
| 日志级别         | error / warn / info / debug / trace                         |

日志级别说明：

- **error** - 仅记录错误
- **warn** - 记录警告和错误
- **info** - 记录一般信息（默认）
- **debug** - 记录调试信息
- **trace** - 记录所有详细信息

## OAuth 认证中心（Beta）

设置 → **认证** Tab

v3.13.0 新增的 **OAuth 认证中心**（Beta）统一管理第三方 OAuth 凭据，目前支持三类账号：

| 账号类型                     | 用途                                            |
| ---------------------------- | ----------------------------------------------- |
| **GitHub Copilot**           | 配合 Copilot 反向代理使用                       |
| **ChatGPT (Codex OAuth)**    | 配合 Codex OAuth 反向代理使用，管理 ChatGPT 账号   |
| **xAI (Grok OAuth)**         | 配合 xAI Responses API 反向代理使用               |

**你可以在这里**：

- 通过 Device Code 流程登录 ChatGPT / GitHub / xAI 账号
- 查看已登录账号列表和认证状态
- 为多账号设置默认账号
- 移除单个账号或一键注销所有账号

xAI 集成使用 OAuth 2.0 Device Authorization Grant，不使用本机回环回调。其网络依赖契约如下：

| 依赖 | 端点 |
| ---- | ---- |
| OpenID discovery | `https://auth.x.ai/.well-known/openid-configuration` |
| 设备授权 | discovery 返回的 `device_authorization_endpoint`（当前为 `https://auth.x.ai/oauth2/device/code`） |
| Token 交换/刷新 | discovery 返回的 `token_endpoint`（当前为 `https://auth.x.ai/oauth2/token`） |
| 推理 | `https://api.x.ai/v1/responses` |
| 模型发现 | `https://api.x.ai/v1/models` |

流程不占用任何本地回调端口；认证和模型请求均复用 CC Switch 的全局出站代理设置。

xAI 集成**不使用**注册给 CC Switch 的 OAuth 应用，而是复用 xAI 官方 Grok CLI 注册的公共 OAuth 客户端身份与 scope（`client_id: b1a00492-073a-47ea-816f-4c329264a828`，包含 `grok-cli:access`）获取账号型 API 访问权限。

> ⚠️ **警告**：这些账号型反向代理使用逆向接入的 OAuth 流程，存在账号与服务条款风险。复用上游产品的 OAuth 客户端可能不受官方支持，并可能导致服务商限制或封禁账号。请在使用前审阅对应服务商条款并自行承担风险；现有 Codex 流程另见 [2.1 添加供应商 → Codex OAuth 反向代理](../2-providers/2.1-add.md#codex-oauth-反向代理claude-供应商) 的完整风险提示。

## 关于页面

设置 → 关于 Tab

### 版本信息

显示当前 CC Switch 版本号，支持：

- 查看发布说明
- 检查更新
- 下载并安装新版本

### 本地环境检查

自动检测已安装的 CLI 工具版本：

| 工具        | 检测内容           |
| ----------- | ------------------ |
| Claude Code | 当前版本、最新版本 |
| Codex       | 当前版本、最新版本 |
| Gemini CLI  | 当前版本、最新版本 |
| Grok Build  | 当前版本、最新版本 |
| OpenCode    | 当前版本、最新版本 |
| OpenClaw    | 当前版本、最新版本 |
| Hermes      | 当前版本、最新版本 |
| Pi          | 当前版本、最新版本 |

点击「刷新」按钮可重新检测；检测到新版时可单独升级，也可以点击「全部升级」。未检测到的工具会显示安装按钮。安装与升级在后台静默执行，按钮上会显示进度，完成后自动刷新版本。已安装但当前环境无法运行（例如 Node.js 版本不够）的工具会标为「已安装·无法运行」。

在 Windows 上，如果在「配置文件目录」里把某个工具的目录设为 WSL 路径，「关于」页会在对应的 WSL 发行版里检测和升级该工具。

### 手动安装命令

该区域默认折叠，点击标题展开，可以一键复制所有工具的安装命令：Claude Code、OpenCode、Hermes 使用官方安装脚本（Claude Code 和 OpenCode 失败时回退到 npm），其余工具使用 npm 安装。Windows 上 Hermes 使用 PowerShell 安装脚本，其余工具使用 npm。各工具的安装命令也可参考 [1.2 安装指南](./1.2-installation.md#可选安装要管理的工具)。
